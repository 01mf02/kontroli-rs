[workspace]
members = [
    "dedukti-parse",
    "kontroli",
    "kocheck",
    "koweb",
]

# do not build koweb by default
default-members = [
    "dedukti-parse",
#   TODO: fix tests in Kontroli!
#   "kontroli",
    "kocheck",
]

[profile.release]
codegen-units = 1
