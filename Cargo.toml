[workspace]
members = [
    "dedukti-parse",
    "kontroli",
    "kofmt",
    "kocheck",
    "koweb",
]

default-members = [
    # for tests
    "dedukti-parse",
    "kontroli",

    # for the main binary
    "kocheck",
]

[profile.release]
codegen-units = 1
